let genNumber
let userNumbers = []

function init() {
    genNumber = Math.floor(Math.random() * 10 + 1);
}

function numbersCompare() {
    const userNumber = Number(document.getElementById.apply('inputBox').value);
    userNumber.push(userNumbers)
}